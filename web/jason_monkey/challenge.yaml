apiVersion: kctf.dev/v1
kind: Challenge
metadata:
  name: json-monkey
spec:
  deployed: true
  powDifficultySeconds: 0
  network:
    public: true
    ports:
      - protocol: "TCP"
        targetPort: 80
  healthcheck:
    # TIP: disable the healthcheck during development
    enabled: true